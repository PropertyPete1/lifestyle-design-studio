name: Social Analytics Probe

# Capability probe for issue #83's analytics collector.
#
# READ ONLY against Metricool — every call is a GET. It creates no post, moves
# no media and publishes nothing, so unlike the long-form probes it carries no
# I_UNDERSTAND_THIS_TOUCHES_LIVE gate.
#
# It answers one question the collector cannot be written without: which
# analytics endpoints does THIS account's plan actually expose, and which of the
# metric fields issue #83 names come back populated? Metricool's own command
# reference does not list youtube under post analytics, and the API is
# plan-gated, so the answer is an empirical one.
#
# Two jobs, both read-only and both manual:
#
#   probe               sweep the analytics endpoints and grade every field
#   collector-dry-run   run the REAL collector against the live API with
#                       DRY_RUN=true — it writes to a scratch dir and issues no
#                       git command, so it proves the mapping end to end
#                       without committing anything
#
# Nothing here is scheduled. It exists to re-answer "what does this plan expose"
# when Metricool changes something or a metric starts looking wrong, and to
# re-verify the collector against live accounts before trusting a change to it.
#
# (While this was being built the jobs were driven by [probe-analytics] and
# [probe-collector] commit-message markers, because workflow_dispatch is only
# selectable once the file is on the default branch. Once merged, dispatch is
# the trigger and the markers are no longer needed.)
on:
  workflow_dispatch:
    inputs:
      days:
        description: 'How many days back to probe'
        required: false
        default: '30'
        type: string
      mode:
        description: 'endpoints = full sweep; followers = follower-source hunt only'
        required: false
        default: 'endpoints'
        type: choice
        options:
          - endpoints
          - followers

# Reports; never commits.
permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Probe which analytics endpoints this account tier exposes
        working-directory: auto-poster
        env:
          PROBE_DAYS: ${{ github.event.inputs.days || '30' }}
          PROBE_MODE: ${{ github.event.inputs.mode || 'endpoints' }}
          METRICOOL_API_TOKEN: ${{ secrets.METRICOOL_API_TOKEN }}
          METRICOOL_BLOG_ID: ${{ secrets.METRICOOL_BLOG_ID }}
          METRICOOL_USER_ID: ${{ secrets.METRICOOL_USER_ID }}
        run: node scripts/probe-social-analytics.mjs

  # Does the collector itself produce the right numbers from the live API?
  # The unit tests pin the mapping against captured rows; this pins it against
  # the account. DRY_RUN writes to a scratch directory and runs no git command.
  collector-dry-run:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install deps
        working-directory: auto-poster
        run: npm ci --no-audit --no-fund
      - name: Collect from the live API, write nothing
        working-directory: auto-poster
        env:
          DRY_RUN: "true"
          METRICOOL_API_TOKEN: ${{ secrets.METRICOOL_API_TOKEN }}
          METRICOOL_BLOG_ID: ${{ secrets.METRICOOL_BLOG_ID }}
          METRICOOL_USER_ID: ${{ secrets.METRICOOL_USER_ID }}
        run: node scripts/collect-social-analytics.mjs
